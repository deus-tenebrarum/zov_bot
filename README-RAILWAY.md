# Деплой на Railway

1. Залей всё в Git и подключи репо к Railway.
2. В Railway → Variables добавь:
   - `BOT_TOKEN` — токен от @BotFather
   - `GAME_URL` — URL приложения (например https://xxx.up.railway.app)
   - `API_URL` — тот же URL
3. Деплой запустится сам.
